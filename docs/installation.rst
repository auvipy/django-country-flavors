============
Installation
============

At the command line::

    $ easy_install countryflavors

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv countryflavors
    $ pip install countryflavors
